import CarScreen from './CarScreen';

const CarConfig = {
  Car: {
    screen: CarScreen,
    navigationOptions: {
      header: null, // Customize header if needed
      title: 'Car'
    }
  }
};

export default CarConfig;
